{#
  automation/templates/definitions/services/ntp_cisco.j2

  Cisco IOS XE — NTP client configuration.
  Negates previous server entries before applying for idempotency.

  Required context:
    ntp_server  (str) : NTP server IP
#}
no ntp server 10.20.20.100
no ntp server 172.20.20.1
ntp server {{ ntp_server }}