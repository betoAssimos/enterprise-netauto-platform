# /etc/ntp.conf
# NTP server configuration for svc-01
# Generated by Ansible — enterprise-netauto-platform
# BusyBox ntpd — simplified config
#
# Uses WSL host (172.20.20.1) as upstream time source.
# WSL host clock is synced from Windows host — accurate and always available.

server 172.20.20.1